{
  "name": "geeknews-crawl",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "crawl": "tsx --env-file-if-exists=.env src/cli/crawl.ts",
    "lint:vault": "tsx --env-file-if-exists=.env src/cli/lint-vault.ts",
    "tag": "tsx --env-file-if-exists=.env src/cli/tag.ts",
    "relate": "tsx --env-file-if-exists=.env src/cli/link.ts",
    "note": "tsx --env-file-if-exists=.env src/cli/note.ts",
    "graph:build": "tsx --env-file-if-exists=.env src/cli/build-graph.ts",
    "sync:favorites": "tsx --env-file-if-exists=.env src/cli/sync-favorites.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "gray-matter": "^4.0.3",
    "p-limit": "^6.1.0",
    "undici": "^7.2.0",
    "yaml": "^2.6.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "engines": {
    "node": ">=20"
  }
}
